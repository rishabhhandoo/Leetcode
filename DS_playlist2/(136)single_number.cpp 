class Solution {
public:
    int singleNumber(vector<int>& nums) {
        //this can be done using xor
        int num=0;
        for(int i=0;i<nums.size();i++)
        {
            num=num^nums[i];
        }
        return num;
    }
};